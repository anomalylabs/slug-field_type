{{ add_asset("streams.js", "field_type.slug::js/jquery.slugify.js") }}
{{ add_asset("streams.js", "field_type.slug::js/input.js") }}

<input
        type="text"
        name="{{ name }}"
        value="{{ value }}"
        class="{{ class }} slug"
        placeholder="{{ placeholder }}"
        data-separator="{{ separator }}"
        >